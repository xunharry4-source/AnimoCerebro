# 测试计划文件 - Phase 1 稳定化

## 1. 测试目标

1. **证明正常情况稳定可用**: 验证 ThinkLoop 全链路在所有认知引擎集成后的数据流转正确性。
2. **证明异常和特殊情况不会把系统拖入失控**: 验证模型提供商故障、插件缺失时的降级与报错机制。
3. **主动发现未知问题**: 通过串行与并发模拟，观察 WorkingMemory 槽位分配是否产生冲突。
4. **为稳定性增强提供证据**: 验证 JSON 序列化安全性，防止 Mock 导致的递归死循环。

## 2. 验证方法详述

- **准备**: 确保 `.venv` 环境激活，依赖项已安装。
- **执行命令**: `./.venv/bin/python -m pytest tests/ -k "(phase1 or think_loop)"`
- **断言点**:
  - `BrainTurnResult` 包含完整的 `context_snapshot`。
  - `llm_tool.generate_json` 调用次数符合预期。
  - 无 `RecursionError` 或 `TypeError` 抛出。

## 3. 用例覆盖与结果回填

| 测试用例 ID | 描述 | 类型 | 验证方法 | 结果 | 执行时间 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| TL-001 | ThinkLoop 正常全链路执行 | 正常 | pytest tests/test_think_loop.py | **通过** (14项) | 2026-04-07 |
| TL-002 | 记忆管理器 (WM) 槽位溢出 | 特殊 | pytest tests/runtime/test_think_loop.py | **通过** | 2026-04-07 |
| TL-003 | 模型提供商网关故障 (Provider Error) | 异常 | pytest tests/test_think_loop.py | **通过** | 2026-04-07 |
| TL-004 | 认知引擎集成 (WorkingMemory/Temporal) | 正常 | tests/test_phase1_e2e_integration.py | **通过** | 2026-04-07 |

## 4. 失败记录 (History)
- **现象**: `TypeError: cannot unpack non-iterable Mock object`
- **原因**: 认知引擎 Mock 返回值未定义，导致解包失败。
- **解决**: 在 `_build_mock_session` 中统一配置元组返回值。
- **判定**: 产品逻辑缺陷（未考虑 Mock 环境下的鲁棒性）。

---

## 5. Phase 2: Subject Evolution 测试计划 (九问驱动主体进化)

### 5.1 测试目标

1. **证明正常情况稳定可用**: 验证 `NineQDrivenObjectiveEngine` 在九问数据完备时能正确导出三类 profile。
2. **证明异常和特殊情况不会把系统拖入失控**: 验证缺失关键问题 snapshot (如 Q8 缺失) 时，引擎是否能正确降级至默认值而不崩溃。
3. **针对收敛规则的临床验证**: 证明资产不足 (Q3 critical) 时，评价权重确实向风险控制收敛，且 re-normalization 后的总和仍为 1.0。
4. **API 端点可用性**: 证明 Web API `/nine-questions/objectives` 返回的数据来自真实的 `active_evolution_result` 且格式正确。

### 5.2 验证方法详述

- **准备**: 确保 `.venv` 环境激活，PYTHONPATH=src。
- **执行命令**: `./.venv/bin/python -m pytest tests/test_phase2_evolution.py`
- **断言点**:
  - `BrainTurnResult.evolution_result` 不能为空。
  - `evaluation_weights` 在 Q3 告警时产生偏移逻辑。
  - Web API 返回 200 代码且 JSON 模式匹配 `Phase2EvolutionResult`。

### 5.3 用例覆盖要求 (正常 / 异常 / 特殊)

| 用例 ID | 描述 | 类型 | 验证数据/命令 | 期望结果 | 结果 | 执行时间 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| EV-001 | 完备九问导出 | 正常 | q8/q9 内容完整 | 输出 profile 包含任务/权重/进化方向 | **通过** | 2026-04-07 |
| EV-002 | 缺失 Q8 snapshot | 异常 | nq_state 无 q8 | mission 降级为 "Idle"，系统不报错 | **通过** | 2026-04-07 |
| EV-003 | Q3 资产不足收敛触发 | 特殊 | q3.asset_status = "critical" | risk_control 权重显著提升，总和 1.0 | **通过** | 2026-04-07 |
| EV-004 | API 跨 session 隔离 | 特殊 | 多 session 环境 | objectives 返回当前 session 对应结果 | **通过** | 2026-04-07 |
| EV-005 | ThinkLoop 持久化验证 | 正常 | transcript 录入 | evolution_result 进入 transcript 物理文件 | **通过** | 2026-04-07 |

---

## 6. Phase 2 演进物理执行记录 (Execution Logs)

### 6.1 执行环境摘要

- **时间**: 2026-04-07 15:00 (Local)
- **命令**: `PYTHONPATH=src .venv/bin/python -m pytest tests/test_phase2_evolution.py`
- **版本**: Python 3.14.3 / Zentex Phase 2 Core

### 6.2 关键断言日志片段

```text
tests/test_phase2_evolution.py::test_ev_001_normal_derivation PASSED
tests/test_phase2_evolution.py::test_ev_002_missing_q8_exception PASSED
tests/test_phase2_evolution.py::test_ev_003_asset_shortage_convergence PASSED
tests/test_phase2_evolution.py::test_ev_004_api_objectives_endpoint PASSED
tests/test_phase2_evolution.py::test_ev_005_think_loop_integration PASSED
```

### 6.3 稳定性证据 (Stability Check)

- **数据流转**: 验证了 NineQuestionState -> Engine -> Session -> API 的全链路数据透传，无解包错误。
- **降级表现**: 在缺少 snapshot 情况下，Engine 正确输出了安全默认值（Idle mission / General Agent role）。
- **数学一致性**: 验证了收敛规则导致的权重偏移在重采样后依然满足 sum=1.0 的约束。

---

## 7. Phase 3: Self-Evolution 测试计划 (受控代码级自我进化)

### 7.1 测试目标

1. **证明正常情况稳定可用**: 验证 `UpgradeExecutionService` 在检测到 `DEGRADED` 插件时能正确发起 `SelfUpgradeProposal`。
2. **证明异常和特殊情况不会把系统拖入失控**: 验证在缺少 `plugin_worker` 或 `openhands` SDK 时，执行流程能正确进入 `FAILED` 状态而非静默失败。
3. **安全边界检测**: 证明 `PluginEvolutionRuntime` 的扫描器能识别候选补丁中的违规代码模式 (如 `import socket`)。
4. **自动化验证套件**: 证明系统能在子进程中物理执行验证命令并准确回填各阶段结果。

### 7.2 验证方法详述

- **准备**: 确保 `.venv` 环境激活，PYTHONPATH=src，且已存在 `base_models.py` 解决循环依赖。
- **执行命令**: `PYTHONPATH=src .venv/bin/python tests/test_phase3_evolution.py` (由执行器自动产生并清理)
- **断言点**:
  - `proposals` 包含目标插件 ID 和 `plugin_degraded` 原因。
  - `scan_for_forbidden_calls` 准确检出 `requests.get` 等模式。
  - `execute_plugin_evolution` 无 worker 时抛出 `RuntimeError`。

### 7.3 用例覆盖 (正常 / 异常 / 特殊)

| 用例 ID | 描述 | 类型 | 验证命令 | 期望结果 | 结果 | 执行时间 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| SV-001 | 缺口自动探测 | 正常 | detect_capability_gap | 识别并生成 DEGRADED 插件提案 | **通过** | 2026-04-07 |
| SV-002 | 安全静态扫描 | 特殊 | scan_for_forbidden_calls | 检出违规导入和网络调用 | **通过** | 2026-04-07 |
| SV-003 | 隔离验证执行 | 正常 | run_verification_suite | 物理执行命令并返回 stdout/stderr | **通过** | 2026-04-07 |
| SV-004 | Fail-Closed 运行保护 | 异常 | execute_plugin_evolution | 无 worker 时阻塞并抛出运行时异常 | **通过** | 2026-04-07 |

---

## 8. Phase 3 物理执行记录 (Execution Logs)

- **时间**: 2026-04-07 15:40 (Local)
- **结论**: 全链路基础组件已调通，循环依赖已通过 `base_models.py` 物理隔离解决。

```text
[Test] Testing Phase 3 Gap Detection...
Detected 1 proposals for test_plugin.
[Test] Testing Phase 3 Code Scanner...
Detected Violations: ["Forbidden pattern 'import socket' detected"]
[Test] Testing Phase 3 Fail-Closed Execution...
Caught expected error: Plugin evolution execution requires a real plugin worker.
All Phase 3 base tests PASSED.
```

---

## 9. Phase 4: Memory Consolidation 测试计划 (睡眠式巩固与治理)

### 9.1 测试目标

1. **证明正常情况稳定可用**: 验证 `ConsolidationEngine` 在周期结束时能生成 `quarantined` 状态的晋升候选。
2. **证明异常和特殊情况不会把系统拖入失控**: 验证 `EnhancedMemoryService.archive_cold` 能正确迁移记录并更新治理状态，防止内存膨胀。
3. **G38 九重校验集成**: 证明 `promote_from_quarantine` 是晋升到 `ACTIVE` 的唯一受控入口。
4. **降级与自动触发**: 验证存储预算检查 `check_and_trigger_automatic_consolidation` 逻辑路径。

### 9.2 验证方法详述

- **准备**: 确保 `.venv` 环境激活，PYTHONPATH=src。
- **执行命令**: `PYTHONPATH=src .venv/bin/python tests/test_phase4_memory.py` (由执行器产生并验证)
- **断言点**:
  - `MemoryPromotionCandidate` 默认状态为 `quarantined`。
  - `archive_cold` 后，治理状态变为 `cold`。

### 9.3 用例覆盖 (正常 / 异常 / 特殊)

| 用例 ID | 描述 | 类型 | 验证命令 | 期望结果 | 结果 | 执行时间 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| MEM-001 | 隔离晋升机制 | 正常 | _synthesize_cycle_with_model | 候选人自动进入 quarantined | **通过** | 2026-04-07 |
| MEM-002 | 冷存储迁移 | 正常 | archive_cold | 状态更新为 cold 并写入 cold_store | **通过** | 2026-04-07 |
| MEM-003 | G38 晋升入口 | 特殊 | promote_from_quarantine | 状态转为 ACTIVE 且包含验证理由 | **通过** | 2026-04-07 |
| MEM-004 | 降级路径保障 | 特殊 | recall | 回退到兼容检索路径 | **通过** | 2026-04-07 |

---

## 10. Phase 4 物理执行记录 (Execution Logs)

- **时间**: 2026-04-07 15:45 (Local)
- **结论**: 补齐了 B8/B8A 文档中的所有功能组件，实现了 G38 隔离区与冷存储机制。

```text
[Test] Testing Phase 4 Memory Quarantine...
Candidate status: quarantined
[Test] Testing Phase 4 Cold Storage...
Record archived to cold storage.
Status: cold
All Phase 4 base tests PASSED.
```

---

## 11. 存储方案动态切换测试计划 (Redis/DiskCache/Kuzu)

### 11.1 测试目标

1. **证明正常情况稳定可用**: 验证 `ZENTEX_CLUSTER_MODE=false` 时，系统启用 `DiskCache` 且能够成功初始化本地 `KuzuDB`。
2. **证明异常和特殊情况不会把系统拖入失控**: 验证在单机模式下，如果代码尝试获取 `Redis` 客户端，系统能够抛出 `RuntimeError` 物理阻断连接，而不是静默失败或导致不可预知的网络超时。
3. **针对集群模式的隔离性验证**: 证明 `ZENTEX_CLUSTER_MODE=true` 时，系统自动跳过本地 `KuzuDB` 初始化，且所有共享状态存储均通过 `Redis` 进行。
4. **验证默认行为**: 证明在缺省环境变量时，系统能安全回落至单机模式。

### 11.2 验证方法详述

- **准备**: 确保 `.venv` 环境激活，PYTHONPATH=src。
- **单机模式命令**: `ZENTEX_CLUSTER_MODE=false ./.venv/bin/python -m pytest tests/common/test_storage_modes.py`
- **集群模式命令**: `ZENTEX_CLUSTER_MODE=true ./.venv/bin/python -m pytest tests/common/test_storage_modes.py`
- **断言点**:
  - 单机模式：`get_redis_client()` 抛出 `RuntimeError`。
  - 单机模式：`EnhancedMemoryService` 包含 `kuzu_adapter`。
  - 集群模式：`get_redis_client()` 成功初始化（若 Redis 可用）。
  - 集群模式：`EnhancedMemoryService` 的 `kuzu_adapter` 为 `None`。

### 11.3 用例覆盖 (正常 / 异常 / 特殊)

| 用例 ID | 描述 | 类型 | 验证命令 | 期望结果 | 结果 | 执行时间 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| STR-001 | 单机模式存储分支 | 正常 | ZENTEX_CLUSTER_MODE=false | 使用 DiskCache, 启用 Kuzu | **通过** | 2026-04-08 |
| STR-002 | 集群模式存储分支 | 正常 | ZENTEX_CLUSTER_MODE=true | 使用 Redis, 禁用 Kuzu | **通过** | 2026-04-08 |
| STR-003 | 非法跨模式调用拦截 | 异常 | 物理调用 get_redis_client (false 模式) | 抛出 RuntimeError | **通过** | 2026-04-08 |
| STR-004 | 环境变数缺省回落 | 特殊 | 无环境变量 | 默认进入单机模式 | **通过** | 2026-04-08 |


---

## 13. Phase 5: Memory Engine v2.0 测试计划 (存储、检索与安全强化)

### 13.1 测试目标

1. **证明正常情况稳定可用**: 验证全链路（存储优化、混合索引、置信度评分、安全隔离）在生产级负载下的稳定性。
2. **证明异常和特殊情况不会把系统拖入失控**: 验证在解密 Key 丢失、索引文件损坏、或非法跨租户访问时的熔断与保护机制。
3. **海量数据检索精度**: 证明在 10W+ 记录场景下，P95 延迟控制在 100ms 内，且语义检索的 Precision@10 达到 0.85 以上。
4. **置信度与深度召回证据**: 证明 Fuzzy Memory 被正确标记为低置信度（< 0.8），且 Deep Recall 能通过 3-hop 图遍历重建决策链上下文。

### 13.2 验证方法详述

- **准备**: 确保 `.venv` 环境包含核心库：`zstandard`, `lz4`, `cryptography`, `msgpack`, `faiss-cpu`, `sentence-transformers`。
- **配置**: 设置 `MEMORY_ENCRYPTION_KEY` 以启用 AES-256-GCM 加密路径。
- **执行命令**: `PYTHONPATH=src .venv/bin/python -m pytest tests/memory/`
- **断言点**:
  - `EnhancedMemoryRecord` 在加密/压缩存储后，反序列化内容与原内容 100% 一致（通过哈希验证）。
  - `IsolationManager` 阻止任何不包含匹配 `tenant_id` 或 ACL 权限的查询。
  - `metrics.py` 成功上报 P95 延迟、缓存命中率及压缩比。

### 13.3 用例覆盖 (正常 / 异常 / 特殊)

| 用例 ID | 描述 | 类型 | 验证数据/命令 | 期望结果 | 结果 | 执行时间 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| ENG-001 | 透明加密/解密 | 正常 | write encrypted -> read back | 物理文件无明文，内存还原正确 | **通过 (1.1)** | 2026-04-08 |
| ENG-002 | 混合搜索精度 (Hybrid) | 正常 | keyword + semantic search | Precision@10 > 0.85, 耗时 < 100ms | 待执行 | - |
| ENG-003 | 非法跨租户查询拦截 | 异常 | query tenant_B with tenant_A creds | 抛出 AccessDenied 异常，记录审计日志 | 待执行 | - |
| ENG-004 | 损坏文件自动修复 | 异常 | delete index file during runtime | 自动触发 Index Rebuild, 恢复检索 | 待执行 | - |
| ENG-005 | 置信度评分偏移特性 | 特殊 | contradicting memories | 冲突记录的 confidence 显著下降 (< 0.5) | 待执行 | - |
| ENG-006 | 深度召回 (3-hop) | 正常 | deep_recall(seed_mem) | 返回 3 跳以内的关联路径及其解释描述 | 待执行 | - |
| ENG-007 | mmap 大文件加载稳定性 | 特殊 | load 500MB shard via mmap | 瞬时 RSS 占用增量 < 50MB (零拷贝) | 待执行 | - |
| ENG-008 | 格式自动探测 (兼容性) | 特殊 | load plain JSONL in binary engine | 自动识别 magic bytes 并按旧格式解析 | **通过 (1.1)** | 2026-04-08 |
