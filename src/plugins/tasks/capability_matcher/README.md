# Task Capability Matcher Plugin

Provides deterministic capability matching for task dispatch:
- exact required capability coverage
- overlap score
- missing capability list

It does not select executors by itself. It only returns matching evidence.
