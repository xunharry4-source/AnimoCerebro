# Task Rule-Based Verification Plugin

Deterministic verification plugin for task results.

It validates structured outputs using simple rules such as:
- required field
- exact value
- minimum string length

This plugin is a `FUNCTIONAL` task plugin and must be executed only through `SystemPluginService`.
