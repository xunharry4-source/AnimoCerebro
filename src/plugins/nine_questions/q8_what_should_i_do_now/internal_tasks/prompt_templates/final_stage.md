Final stage: synthesize Q8 internal ObjectiveProfile only. Return strict JSON with exactly one top-level key: `ObjectiveProfile`. If the strategic mission is unavailable or too vague, still return `ObjectiveProfile` and put the uncertainty in `ObjectiveProfile.pause_conditions`; do not invent unrelated internal work. `ObjectiveProfile.current_mission` must inherit context.Strategic_Mission_&_User_Intent.current_total_goal when it is available. `ObjectiveProfile.basis_and_traceability` may contain exactly these four keys only: `q1_environment_bases`, `q2_asset_support_bases`, `q3_role_alignment`, and `q7_boundary_checks`; do not merge evidence from different questions and do not put objective or condition fields inside it. `ObjectiveProfile.primary_objectives`, `ObjectiveProfile.secondary_objectives`, `ObjectiveProfile.completion_conditions`, `ObjectiveProfile.pause_conditions`, and `ObjectiveProfile.escalation_conditions` must be direct sibling fields of `ObjectiveProfile.basis_and_traceability`, not children of `basis_and_traceability`. Q2 traceability may name the concrete cognitive or functional assets found in the inventory, but `ObjectiveProfile.primary_objectives` and `ObjectiveProfile.secondary_objectives` must stay pure abstract business intent and must not name concrete tools. Use context.Environment_State and context.Role_Profile only as background, and treat context.Internal_Cognitive_Assets and context.Internal_Brain_Organs_State as the only internal objects that may be optimized. Every objective must remain read_only=True and side_effect_free=True in meaning, and must stay in pure cognitive domains such as memory organization, reflection, root-cause reasoning, strategy patch synthesis, conflict audit, or safe planning. Q8 internal generation only decides what internal cognition should happen; downstream（Q9） decomposition, persistence, and scheduling are owned by the 任务中心 after this LLM output is stored. Do not output `internal_cognitive_tasks`, `task_name`, `task_description`, `task_goal`, `task_creation_reason_and_basis`, `target_engine_or_organ`, `cognitive_plugin_ref`, `execution_parameters`, `expected_receipt_type`, `internal_objectives`, `objective_profile`, `task_queue`, `executor_type`, `target_id`, `required_capabilities`, `ActionExecutionReceipt`, markdown, or explanatory prose.
